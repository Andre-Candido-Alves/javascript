function soma(n1=0, n2=0){
return n1 + n2

}

console.log(soma(5)) //chamada + execução na tela. Sempre chame a função em uma variavel ou o console.log que é praticamente uma variavel e ja vai executar na tela

//estamos pedindo 2 valores se um não for digitado ele recebe undefined e o retorno passa a ser Nan(not a number). Se vc colocar =0 na frente do parametro ele considera 0 caso não seja definido